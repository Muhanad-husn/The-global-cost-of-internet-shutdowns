"""Streamlit dashboard for internet shutdowns 2019–latest.

# TODO: implemented in Session 6.

Headline deliverable: world map + country drill-down + time series, driven off
``data/processed/analytic_dataset_<snapshot>.parquet``. Reuses the viz helpers
from ``src/internet_shutdowns/viz.py`` — do not build a parallel viz pipeline.

A persistent banner cell will surface the Top10VPN methodology caveat (see
CLAUDE.md): cost figures are reported, never endorsed.
"""
